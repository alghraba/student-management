pipeline {
    agent any
    tools {
        jdk 'JAVA_HOME' // This name must match the name in Global Tool Configuration
        maven 'M2_HOME' 
    }
    stages {
        // ... your stages
    }
}
